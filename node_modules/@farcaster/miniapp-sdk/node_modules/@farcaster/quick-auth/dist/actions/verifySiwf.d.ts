import { Config } from "../config.js";
import { VerifySiwf } from "../endpoints/index.js";
import { GlobalErrorType, InvalidSiwfError, ResponseError } from "../errors.js";
export declare namespace verifySiwf {
    type Options = VerifySiwf.RequestBody;
    type ReturnValue = {
        /**
         * JWT issued by the Quick Auth server. Verify it on your backend with
         * `verifyJwt`. If you want to inspect the contents without verifying it
         * you can use `decodeJwt`.
         */
        token: string;
    };
    type ReturnType = Promise<ReturnValue>;
    type ErrorType = InvalidSiwfError | ResponseError | GlobalErrorType;
}
export declare function verifySiwf({ origin }: Config, options: verifySiwf.Options): verifySiwf.ReturnType;
