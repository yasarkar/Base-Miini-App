import { z } from "zod/v4";
export const requestBodySchema = z.object({
    message: z.string(),
    domain: z.string(),
    signature: z.string(),
    acceptAuthAddress: z.boolean().optional().default(true)
});
